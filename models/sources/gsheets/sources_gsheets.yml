version: 2

sources:
    - name: gsheets
      description: Google Sheets connector data
      database: DA_TEAM_TEST_RAW
      #database: |
      #    "{{target.name}}_RAW"
      schema: FIVETRAN_GSHEETS
      tables:
        - name: zipcodes
        - name: exceptions
          identifier: exceptions